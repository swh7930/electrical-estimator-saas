name: Ops — Monthly Checklist
on:
  schedule:
    - cron: "10 14 1 * *"    # 1st of month at 14:10 UTC
  workflow_dispatch: {}
permissions:
  issues: write
jobs:
  open:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: peter-evans/create-issue-from-file@v4
        with:
          title: "🗓️ Monthly Ops Checklist"
          content-filepath: .github/ISSUE_TEMPLATE/ops-monthly.md
          labels: ops,monthly
          assignees: swh7930
