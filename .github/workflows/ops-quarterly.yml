name: Ops — Quarterly Checklist
on:
  schedule:
    - cron: "15 14 1 1,4,7,10 *"   # First day of each quarter at 14:15 UTC
  workflow_dispatch: {}
permissions:
  issues: write
jobs:
  open:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: peter-evans/create-issue-from-file@v4
        with:
          title: "🧭 Quarterly Ops Checklist"
          content-filepath: .github/ISSUE_TEMPLATE/ops-quarterly.md
          labels: ops,quarterly
          assignees: swh7930
