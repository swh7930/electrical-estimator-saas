name: Ops — Weekly Checklist
on:
  schedule:
    - cron: "5 14 * * 1"     # Mondays at 14:05 UTC
  workflow_dispatch: {}
permissions:
  issues: write
jobs:
  open:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: peter-evans/create-issue-from-file@v4
        with:
          title: "📅 Weekly Ops Checklist"
          content-filepath: .github/ISSUE_TEMPLATE/ops-weekly.md
          labels: ops,weekly
          assignees: swh7930
