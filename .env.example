# App mode
APP_ENV=development

# Flask secret & DB
SECRET_KEY=change-me
DATABASE_URL=postgresql+psycopg2://postgres:YOURPASS@localhost/electrical_estimator

# Testing DB (optional)
TEST_DATABASE_URL=sqlite:///:memory:

# Mail (optional)
MAIL_SERVER=
MAIL_PORT=587
MAIL_USE_TLS=1
MAIL_USERNAME=
MAIL_PASSWORD=

# Security & sessions
SESSION_LIFETIME_DAYS=7
SESSION_COOKIE_SAMESITE=Lax

# Limiter (optional)
RATELIMIT_DEFAULT=200 per day;50 per hour
